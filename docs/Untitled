Running the SOA tests:


[1 tool called]


The error is that `createTestCompany` uses `tenantId` (camelCase) instead of `tenant_id` (snake_case). Checking the test helpers and fixing it:


[1 tool called]


Fixing `createTestCompany`: `...companyData` may include `tenantId` (camelCase), which overwrites `tenant_id`. Ensuring we use `tenant_id` and prevent camelCase from overriding it:


[2 tools called]


Checking for other places where `tenantId` might be passed incorrectly:


[2 tools called]


Updating calls to use `tenant_id` instead of `tenantId`:


[2 tools called]


Running the tests again:


[1 tool called]


Fixing the same issue with `companyId` vs `company_id` in `createTestCase` and `createTestSOACase`:


[3 tools called]


Fixing the variable name conflict in `createTestSOACase`:


[3 tools called]


Fixing the same issue in `createTestSOALine`:


[3 tools called]