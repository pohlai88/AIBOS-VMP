import express from 'express';

const clientRouter = express.Router();

// Stub router for client portal. Add client SSR and partial routes here progressively.
// Empty router is safe; non-matching requests fall through to subsequent handlers.

export default clientRouter;
