import express from 'express';

const vendorRouter = express.Router();

// Stub router for vendor portal. Add vendor SSR and partial routes here progressively.
// Empty router is safe; non-matching requests fall through to subsequent handlers.

export default vendorRouter;
